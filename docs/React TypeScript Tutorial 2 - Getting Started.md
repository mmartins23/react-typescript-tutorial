# React TypeScript Tutorial 2 - Getting Started

To create a new React app with TypeScript:

1. Install Node.js and npm.
2. Open your terminal and run: `npx create-react-app my-ts-app --template typescript`.
3. Navigate to the project directory (`cd my-ts-app`) and start the development server (`npm start`).